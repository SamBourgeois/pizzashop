<header>
    <div class="logo">
        <img src="images/pizzashop.png">
    </div>
    <div class="menu">
        {% include 'menu.twig' %}
    </div>
    {% if loggedIn == true %}
        <div class="ingelogd">
            <p>ingelogd als <a href="account.php">{{gebruikersnaam}}</a></p>
        </div>
    {% endif %}
    <div class="login">
        {% if isAdmin == true %}
            {% include 'adminmenu.twig' %}
        {% elseif loggedIn == true %}
            {% include 'dropdownMenu.twig' %}         
        {% elseif not(loginHidden == true) %}
            {% include 'login.twig' %}
        {% endif %}
        
    </div>

</header>